#include "AbstractNode.h"
#include "../decoders.h"

using namespace jsxbin;

namespace jsxbin::nodes {

    class SubNode : AbstractNode {
    public:
        explicit SubNode(ScanState &scanState) : AbstractNode(scanState) {}

        void parse() override {

        }

        string jsx() override {

        }

    private:

    };
}